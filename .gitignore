data/*
qr.png
.env

# build artifacts
*.exe

# local launcher (contains user-specific paths)
*.vbs

# stray scratch files at repo root (this is a Go project — no Python at root)
/*.py

# Windows stackdumps
*.stackdump

# Editor/build backup files
*~

# Claude-generated scratch files (legacy — new ones go to data/claude-work/)
/*.txt
/*.md